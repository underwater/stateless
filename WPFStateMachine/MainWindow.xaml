<Window x:Class="WPFStateMachine.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFStateMachine"
        Title="{Binding Title}"
        mc:Ignorable="d"
        Height="450" Width="800">
    <Window.DataContext>
        <local:IncidentViewModel/>
    </Window.DataContext>
    <StackPanel Orientation="Vertical">
        <Button Command="{Binding CreateCommand}">Create</Button>
        <Button Command="{Binding ValidateCommand}">Validate</Button>
        <Button Command="{Binding ExecuteCommand}">Excecute</Button>
        <Button Command="{Binding AbandonCommand}">Abandon</Button>
        <Button Command="{Binding EditCommand}">Edit</Button>
        <Button Command="{Binding DeleteCommand}">Delete</Button>
        <Button Command="{Binding ArchiveCommand}">Archive</Button>
    </StackPanel>
</Window>
